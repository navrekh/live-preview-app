# AppDev Environment Configuration
# =================================
# Copy this file to .env and fill in your values

# AWS API URL (Required)
# Your EC2 backend endpoint - replace with your actual EC2 public IP
# Example: http://13.234.56.78:3001/api
VITE_AWS_API_URL=http://<EC2-PUBLIC-IP>:3001/api

# AWS Region
VITE_AWS_REGION=ap-south-1

# AWS Cognito (Optional - if using Cognito for auth)
VITE_COGNITO_USER_POOL_ID=ap-south-1_xxxxxxxxx
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_DOMAIN=your-domain.auth.ap-south-1.amazoncognito.com

# AWS S3 (Optional - for file uploads)
VITE_S3_BUCKET=appdev-builds
VITE_S3_REGION=ap-south-1

# Payment Gateway (Razorpay for INR payments)
# Get your keys from https://dashboard.razorpay.com/app/keys
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx

# Backend endpoints needed:
# POST /api/payments/create-order - Creates Razorpay order
# POST /api/payments/verify - Verifies payment and adds credits
# POST /api/apps/chat - AI chat for app building

# ===========================================
# BACKEND SECRETS (store on AWS, NOT here!)
# ===========================================
# ANTHROPIC_API_KEY=sk-ant-xxxxx  <- Store this on your AWS backend
# 
# The frontend calls POST /api/apps/chat
# Your AWS backend uses the Claude API key to call Anthropic
# This keeps your API key secure and never exposed to the browser
